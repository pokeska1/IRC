MANDATORY:
1. Communication between client and server must be done via TCP/IP(v4) or (v6) - show this
2. To verify that your server correctly uses everything you send, 
an initial test can be done with nc (Use ctrl+d to send parts of the command):
\$> nc 127.0.0.1 6667
com^Dman^Dd
\$>
This will allow you to first send the letters com, man, d\n.
 You must first aggregate the packets to rebuild the command command to handle it.
3. Choose IRC client as a reference
4. You must be able to:
	4a. authenticate,	PASS
	4b. set a nickname,	NICK
	4c. a username,		USER
	4d. join a channel,	JOIN
	4e. send and receive private messages using this client	PRIVMSG
5. You must have operators and regular users
6. Some operator’s specific actions/commands
7. Commands TODO:
	PASS    +   сделано
	NICK    +   сделано
	USER    +   сделано
	TOPIC   +   сделано
	VERSION +   сделано
	INFO    +   сделано
	NOTICE  +   сделано
	KICK    +   сделано
	PRIVMSG +   сделано     ?   jmarian
    JOIN    +   сделано     ?   jmarian
    MODE    +   делается        rmerrie
    INVITE  +   делается        rmerrie
    PART    +   делается        epilar
    ISON    +   делается        jmarian
    WHO     -
    PING    -



BONUS:
1. File transfer
2. A bot

CHECKLIST:
1. poll (or equivalent) must be only one


заметочки
1) JOIN
    //1.1)при создании каналов неверно отображается список юзеров
    //1.2)Настроить выдачу @ в списке юзеров канала
    //1.3) фалг i при joine и разрешение на  JOIN через список инвайтов
    //1.4)макс 10 каналов
    //1.5) при ручном множественном джойне не создается второй канал в текстуале
    //1.6)исправил доступ по паролям. теперь пока не введешь правильyый пароль в канале быть
    //1.7) при выдачеесписка пользователей ставить @ у оперов.
    1.8)
2) PRIVMSG
    //2.1)Множественное Приватное Сообщение  -- сделал.
    //2.2)множественное канальное сообщение -- сделал.
    2.3)
3)KICK
    //3.1)Заблокировать Возможно блокировки самого себя.
4)NOTIC
    4.1)сделал под копирку с привмсг , оставил альтернативный вариант.
    4.2)


